---
Назва: Інструкція з додавання нової мови
Опис: Покроковий посібник для розширення локалізації та фонетичної підтримки в Slovko.
Тип: інструкція
Дата Створення: 2026-02-01
Дата Оновлення: 2026-02-01
---

# 🌍 Як додати нову мову в Slovko

Ця інструкція допоможе вам інтегрувати нову мову в систему. Слідуйте крокам і оновлюйте цей документ, якщо знайдете нові нюанси.

## 📝 Крок 1: Оновлення типів та конфігурації

1.  **Типи:** Відкрийте `src/lib/types.ts`:
    - Додайте код нової мови до типу `Language` (наприклад, `'el'`).
    - Додайте назву мови до об'єкта `LANGUAGE_NAMES`.
    - Додайте емодзі прапора до об'єкта `LANGUAGE_FLAGS`.
2.  **i18n реєстрація:**
    -   Відкрийте `src/lib/i18n/init.ts`.
    -   Додайте `register('код', () => import('./translations/код.json'));`.
    -   Додайте код мови до масиву `SUPPORTED_LOCALES`.
3.  **Переклад інтерфейсу:** Створіть файл `src/lib/i18n/translations/код.json` (скопіюйте з `en.json` як шаблон).

## 🗂️ Крок 2: Дані перекладів та транскрипцій

1.  **Структура папок:** Створіть нові папки:
    -   `src/lib/data/translations/код/levels/`
    -   `src/lib/data/translations/код/topics/`
    -   `src/lib/data/translations/код/phrases/`
2.  **Наповнення:** Скопіюйте JSON файли з англійської мови та перекладіть їх.
    *Порада: Використовуйте `scripts/maintenance/smart_translate.js` для автоматизації (якщо він адаптований).*
3.  **Транскрипції:** Якщо мова підтримує фонетичний режим, створіть папку `src/lib/data/transcriptions/код/`.

## 🔊 Крок 3: Фонетична логіка та IPA

1.  **Мапінг IPA:** Створіть файл `src/lib/data/ipa-maps/map-ipa-to-код.ts`. Цей файл визначає, як звуки IPA відображаються на символи вашої мови.
2.  **Сервіс транскрипції:** Створіть `src/lib/services/transcription/код.ts`. Тут описуються правила читання (якщо вони алгоритмічні).
3. **Реєстрація сервісу:** Додайте нову мову в `src/lib/services/transcriptionService.ts`:
    - Імпортуйте генератор: `import { generateКодIPA } from "./transcription/код";`.
    - Додайте умову в `generateRulesIPA`: `if (lang === 'код') return generateКодIPA(text);`.
    - Додайте код у функцію `isLatin`, якщо мова використовує латиницю.

## 🎨 Крок 4: Графічні асети та UI

1.  **Прапор:** Додайте іконку прапора в `static/flags/код.svg`.
2.  **Відображення в UI:** Переконайтеся, що нова мова з'явилася в налаштуваннях.
    - Перевірте `src/lib/components/settings/LanguageSettings.svelte`. Він має імпортувати `LANGUAGES` з `$lib/i18n/init`. Якщо там захардкоджений масив — замініть його на динамічний.
3.  **Вибір голосу:** Перевірте `src/lib/components/settings/VoiceSelectionModal.svelte`. Система автоматично шукає доступні системні голоси для кодів мов, але варто перевірити сумісність.

## ✅ Крок 5: Перевірка

1.  Запустіть `npm run dev`.
2.  Перейдіть в налаштування та спробуйте змінити мову інтерфейсу.
3.  Спробуйте змінити мову карток (Source/Target).
4.  Перевірте роботу озвучки (TTS).

---

> 💡 **Примітка для розробника:** Якщо під час додавання мови ви виявили, що якийсь файл потрібно редагувати, а його немає в цьому списку — **негайно оновіть цю інструкцію!** Це принцип живого документа.
