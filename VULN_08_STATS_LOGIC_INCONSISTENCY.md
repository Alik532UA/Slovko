# Вразливість: Логічна несумісність при агрегації статистики (VULN_08)

## 📝 Опис
У `SyncService.svelte.ts` метод `mergeLevelStats` об'єднує статистику по кожному рівню, використовуючи `Math.max` для кожного поля окремо.

```typescript
merged[lvl] = {
    totalCorrect: Math.max(l.totalCorrect, c.totalCorrect || 0),
    totalAttempts: Math.max(l.totalAttempts, c.totalAttempts || 0),
    // ...
};
```

## 🔴 Ризики
1. **Пошкодження статистики:** Якщо на одному пристрої користувач зіграв 10/10 (100% точність), а на іншому 5/20 (25% точність), то після злиття він отримає 10/20 (50% точність).
2. **Нереальні стани:** Сумарна кількість правильних відповідей може перевищити загальну кількість спроб або навпаки, що виглядає як помилка програми для користувача.

## 🛠 Рекомендації
1. **Atomic Object Sync:** Замість злиття окремих полів, слід порівнювати `updatedAt` для всього об'єкта `LevelStats` і вибирати той, що є новішим (або має більше прогресу).
2. **History-First Sync:** Відновлювати агреговану статистику на основі суми записів з колекції `history`, яка є Single Source of Truth для активності за день.

---

## ✅ Статус: Виправлено (05.02.2026)
Впроваджено стратегію **Atomic Object Sync** для статистики рівнів у `SyncService` та `statisticsService`. 
1. **Цілісність даних:** Замість незалежного використання `Math.max` для кожного поля (що призводило до "розмивання" точності), тепер при злитті вибирається цілий об'єкт статистики рівня, який має найбільший прогрес (`totalCorrect`).
2. **Синхронізація станів:** Це гарантує, що показники точності, кращих стріків та спроб завжди відповідають один одному, запобігаючи виникненню статистично неможливих станів.

