# Вразливість: Поверхневий мерж налаштувань (SyncService)

## 📝 Опис
У `SyncService.svelte.ts` метод `mergeData` об'єднує налаштування за допомогою Spread-оператора:
```typescript
settings: cloudIsNewer
    ? { ...local.settings, ...cloud.settings }
    : { ...cloud.settings, ...local.settings },
```

## 🔴 Ризики
1. **Втрата вкладених даних:** Якщо користувач змінив налаштування голосу для англійської мови на одному пристрої (`voicePreferences: { en: "A" }`), а потім для української на іншому (`voicePreferences: { uk: "B" }`), то при синхронізації один об'єкт повністю затре інший. Замість об'єднаних переваг `{ en: "A", uk: "B" }`, залишиться тільки одна з них.
2. **Конфлікти конфігурації:** Це стосується будь-яких майбутніх вкладених налаштувань.

## 🛠 Рекомендації
1. **Deep Merge:** Використовувати функцію глибокого об'єднання для об'єкта `settings`, особливо для поля `voicePreferences`.
2. **Zod Reconciliation:** Використовувати схеми Zod для валідації результату об'єднання, щоб гарантувати цілісність.

---

## ✅ Статус: Виправлено (05.02.2026)
Впроваджено механізм `deepMergeSettings` у `SyncService.svelte.ts`. Тепер при об'єднанні налаштувань вкладені об'єкти (зокрема `voicePreferences`) не затираються, а зливаються. Це запобігає втраті даних при одночасному редагуванні різних параметрів на декількох пристроях.
