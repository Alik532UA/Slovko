node_modules
dist
build
.svelte-kit
static

# Спільна таблиця сусідніх сайтів: той самий файл лежить у восьми репозиторіях,
# і форматується він за конфігом того, звідки походить (adoptananimal). Дати
# тутешньому prettier переписати лапки означало б спричиняти діфф на кожній
# синхронізації там, де вміст не змінився.
src/lib/siblings.ts
