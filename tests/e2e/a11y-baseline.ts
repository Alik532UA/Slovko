/**
 * База машинно-виявних порушень WCAG (ACCESSIBILITY-v8 § 10.1.1).
 *
 * Поріг — ЧИСЛО, що лише спадає, і перелік відомих id, а не літеральний нуль.
 * `toEqual([])` червоніє від щотижневого оновлення `axe-core` — нові правила
 * приходять у мінорних версіях, — тобто це гейт, який доводиться вимикати, а
 * вимкнений гейт гірший за відсутній (той самий клас, що `off` у ESLint).
 *
 * Перелік id потрібен ОКРЕМО від числа: без нього нове порушення сховалося б за
 * лімітом, якби старе тим часом зникло.
 *
 * Заміряно 2026-08-16 командою `npx playwright test a11y --project=chromium`.
 * Число — це кількість порушених ПРАВИЛ; вузлів більше:
 *
 *   | стан    | color-contrast | nested-interactive | target-size |
 *   |---------|---------------:|-------------------:|------------:|
 *   | home    |              1 |                  5 |           3 |
 *   | levels  |              7 |                  6 |           5 |
 *   | profile |              5 |                  6 |           3 |
 *
 * ## Перезаміряно 2026-08-28: 3/3/3 → 1/2/2, і `target-size` зник цілком
 *
 * Дві причини з трьох виявилися ОДНИМ дефектом і одним рядком CSS.
 *
 * **`nested-interactive` і більша частина `target-size`** — це був
 * `BaseTooltip`: обгортка несла `role="button"`, `tabindex="0"` і
 * `aria-label`, не маючи жодного обробника натискання. Усередині майже завжди
 * стоїть справжня кнопка з тим самим підписом, тож виходила фокусована
 * оболонка навколо фокусованої кнопки — і зайва мертва зупинка табуляції на
 * КОЖНІЙ підказці. Тепер обгортка отримує фокус лише тоді, коли всередині
 * нема чого фокусувати (дві плитки статистики), і описує їх через
 * `aria-describedby`, а не вдає кнопку.
 *
 * **Решта `target-size`** — кнопки «+»/«−» у сітках рівнів і плейлистів. У
 * стилях стояло `width: 24px`, а axe міряв 18: типовий `flex-shrink: 1`
 * стискав їх у вузькій картці, тобто оголошене й фактичне розходилися мовчки.
 * Додано `flex-shrink: 0`.
 *
 * ## Перезаміряно 2026-09-02: `nested-interactive` зник цілком
 *
 * Тут стояло, що він «лікується розміткою модалки, а не атрибутом». Виявилося
 * навпаки — саме атрибутами, і це був той самий дефект, що в `BaseTooltip`,
 * тільки на чотирнадцяти вікнах: `.modal-backdrop` у `BaseModal` ніс
 * `role="button"`, `tabindex="0"` і `aria-label="Закрити"`, маючи ВСЕРЕДИНІ
 * усе вікно з власними кнопками. Читалка оголошувала ціле вікно як «Закрити,
 * кнопка», а перед кожним вікном стояла мертва зупинка табуляції.
 *
 * Клік мимо лишився — з клавіатури вікно закривають Escape і кнопка «×».
 *
 * Лишилося:
 *
 *   * `color-contrast` — усі три стани.
 *
 * ## Перезаміряно 2026-09-02 (друге): вузлів `color-contrast` 14 → 7
 *
 * Тут стояло, що потрібен «окремий прохід із заміром кожної теми». Прохід
 * зроблено, і він тепер живе гейтом: `src/contrast.test.ts` розв'язує граф
 * токенів у ЧОТИРЬОХ темах статично — те, чого axe не вміє в принципі, бо
 * бачить лише активну тему й лише стан без наведення.
 *
 * Перший його результат: `--text-secondary` у темній половині був `#a0a0a0`
 * і давав 3,61 на картці. Один рядок у палітрі (`#b8b8b8`, 4,75) прибрав
 * СІМ вузлів із чотирнадцяти: `levels` 6 → 2, `profile` 7 → 4.
 *
 * Число порушених ПРАВИЛ від цього не змінилося — воно й далі 1/1/1, бо
 * `color-contrast` лишається в кожному стані. Це і є межа цієї бази: вона
 * рахує правила, а не вузли, тож поліпшення всередині правила їй невидиме.
 * Числа вузлів звіряти нічим, тому вони тут і не стоять таблицею — за
 * пофарбовані пари відповідає `contrast.test.ts`, де кожна під гейтом.
 *
 * ## Перезаміряно 2026-09-10: вузлів на `profile` 4 → 3
 *
 * Тут стояло, що серед них «дві мітки вкладок профілю (2,06 і 2,62 — колір
 * приглушений прозорістю поверх темного тла)». Причина виявилася не в кольорі:
 * гостю перемикач вкладок приходив із `disabled` на ОБОХ вкладках, тобто це
 * був контрол, який не працює, підсвічує «Друзі» як обрану при формі входу
 * нижче й дає дві мертві зупинки табуляції. `opacity: 0.3` на ньому — наслідок,
 * а не хвороба. Перемикач гостю більше не малюється, і обидва вузли зникли
 * разом із ним.
 *
 * Число порушених ПРАВИЛ від цього знову не змінилося: `color-contrast`
 * лишається в кожному стані через акцент.
 *
 * Що лишилося на трьох станах: білий текст на акценті (`.level-btn`,
 * `.action-btn.primary`, `.btn-primary` — рішення про айдентику) і `.btn-link`
 * «Відновлення пароля» (акцент як ТЕКСТ на картці, 2,58).
 *
 * `target-size` не лишилося ЖОДНОГО — на жодному з трьох станів. Обидві
 * причини були в тих двох виправленнях, і жодна з них не була про «дрібні
 * контроли в кутах», як тут стояло раніше.
 *
 * `button-name` тут БУВ (2 вузли на `levels`) і закритий тим самим комітом, що
 * додав цей файл: шість кнопок «+»/«−» у сітках рівнів і плейлистів отримали
 * `aria-label` зі словника.
 *
 * Правило супроводу: обидва значення тільки зменшуються. Виправив порушення —
 * опусти число ТИМ САМИМ комітом, інакше воно нічого не тримає.
 */
export const A11Y_BASELINE: Record<string, number> = {
	home: 1,
	levels: 1,
	profile: 1
};

export const A11Y_KNOWN: Record<string, string[]> = {
	home: ['color-contrast'],
	levels: ['color-contrast'],
	profile: ['color-contrast']
};
