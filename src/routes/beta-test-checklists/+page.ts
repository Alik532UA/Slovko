/**
 * SSR тут УВІМКНЕНО, на відміну від ігрової сторінки.
 *
 * Ігровий маршрут вимикає SSR (`+page.ts` у корені), бо гра залежить від
 * сховища браузера, — і через це його `<svelte:head>` не потрапляє в
 * пререндер, а заголовок і опис довелося класти статично в `app.html`.
 *
 * Чеклист сховища на етапі рендера не читає, тож його `noindex` доїжджає в
 * зібраний HTML — а саме це й перевіряє `scripts/check-build.mjs`
 * (BETA-CHECKLIST-v8 § 5.5). З `ssr = false` мета-тег існував би лише після
 * гідратації, тобто для пошукового робота його б не було зовсім.
 */
export const prerender = true;
