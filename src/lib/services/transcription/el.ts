/**
 * Greek (el) IPA transcription rules
 */
export function generateElIPA(text: string): string {
	// Тимчасово повертаємо порожню транскрипцію або базову логіку
	// В ідеалі тут мають бути правила читання грецької мови
	return text.toLowerCase();
}
