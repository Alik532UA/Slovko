# Вразливість: Race Condition у SyncService (Lost Update) (VULN_06)

## 📝 Опис
У файлі `src/lib/firebase/SyncService.svelte.ts` метод `handleCloudUpdate` блокує обробку вхідних даних з хмари, якщо в цей момент триває процес вивантаження (`isUploading`).

```typescript
private handleCloudUpdate(cloudData: any) {
    if (this.isUploading || !auth.currentUser) return;
    this.isDownloading = true;
    // ...
}
```

## 🔴 Ризики
1. **Втрата даних (Lost Update):** Якщо користувач змінив дані на одному пристрої та почав синхронізацію, а в цей же момент прийшли зміни з іншого пристрою, вони будуть просто проігноровані. 
2. **Десинхронізація:** Локальний стор залишиться зі старими даними, але `updatedAt` може бути оновлено під час наступного завантаження, що зробить хмарні дані "старішими" в майбутньому.

## 🛠 Рекомендації
1. **Queuing:** Замість негайного повернення (`return`), ставити вхідні дані в чергу на обробку після завершення `isUploading`.
2. **Version Matching:** Порівнювати `updatedAt` перед кожним записом і використовувати транзакції Firestore для гарантування, що ми не затираємо новіші дані.
