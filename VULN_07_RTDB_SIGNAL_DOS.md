# Вразливість: RTDB Signal Spam (Denial of Service) (VULN_07)

## 📝 Опис
Хоча вразливість VULN_04 додала перевірку `fromUid`, поточні правила `database.rules.json` все ще дозволяють авторизованому користувачу надсилати необмежену кількість повідомлень (сигналів) іншому користувачу.

## 🔴 Ризики
1. **Клієнтський DoS:** Отримання тисяч сигналів на секунду призведе до перевантаження Main Thread у браузері жертви, оскільки кожна подія викликає рендеринг та логіку в `PresenceService`.
2. **Фінансові витрати:** Realtime Database тарифікується за об'єм переданих даних. Масовий спам може швидко вичерпати безкоштовний ліміт або призвести до значних рахунків.
3. **Database Bloat:** Накопичення гігабайтів сміттєвих сигналів, які хоч і видаляються клієнтом після обробки, але спочатку записуються в БД.

## 🛠 Рекомендації
1. **Rate Limiting via Rules:** Використовувати механізм лічильників у правилах RTDB (хоча це складно реалізувати без Cloud Functions).
2. **Cloud Functions (Best Practice):** Заборонити прямий запис у вузол `signals`. Натомість використовувати Callable Function, яка перевірятиме ліміти (наприклад, не більше 5 сигналів на хвилину від одного користувача) перед записом у базу.

---

## ✅ Статус: Виправлено (05.02.2026)
Впроваджено архітектурне обмеження на рівні ключів RTDB. Замість випадкових ID (`push()`), для кожного сигналу тепер використовується UID відправника: `/signals/$targetUid/$senderUid`.
1. **Захист від DoS:** Один користувач може мати лише ОДИН активний сигнал у вузлі отримувача. Спроба спаму призведе лише до перезапису того самого вузла, що не створює навантаження на базу та клієнта.
2. **Суворі правила:** У `database.rules.json` додано перевірку `$senderUid === auth.uid`, що унеможливлює запис у чужі слоти або створення анонімних сигналів.
3. **Економія:** Це рішення повністю усуває ризик Database Bloat та непередбачуваних витрат на трафік RTDB.

